enum LastButtonPressed { LEFT, RIGHT, ROTATE_LEFT, ROTATE_RIGHT, NONE }
enum MoveDir { LEFT, RIGHT, DOWN }

const int BOARD_WIDTH = 15;
const int BOARD_HEIGHT = 20;
const double POINT_SIZE = 22; // size is pix

const int GAME_SPEED = 300;

const double GAME_WIDTH = 330;
const double GAME_HEIGHT = 440;
